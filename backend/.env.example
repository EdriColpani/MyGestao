DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres"
JWT_SECRET="troque-por-uma-string-longa-e-aleatoria"
PORT=3333

# O backend carrega SEMPRE este arquivo: backend/.env (mesmo se o cwd for outra pasta).
# Copie a URI em: Supabase > Project Settings > Database > Connection string (sem SEU_REF nem placeholders).

# Obrigatorio quando o front nao e localhost (ex.: EC2). Lista separada por virgula.
# Deve ser EXATAMENTE a URL que aparece na barra do navegador (com porta).
CORS_ORIGINS="http://54.232.189.111:3000,http://localhost:3000"
