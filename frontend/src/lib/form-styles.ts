/**
 * Classe partilhada: altura mínima para toque, `text-base` (evita zoom no iOS),
 * fundo branco e `color-scheme: light` para o input `type="date"` não parecer bloco cinzento.
 */
export const formControlClass =
  "min-h-[2.75rem] w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-base text-slate-900 shadow-sm [color-scheme:light]";
